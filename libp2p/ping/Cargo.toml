[package]
name = "ping"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.55.0", features = ["async-std", "tcp", "tls", "dns", "noise", "yamux", "ping"] }
futures = "0.3.31"
