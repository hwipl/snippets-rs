[package]
name = "ping"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.52.0", features = ["ping", "tcp", "dns", "async-std", "websocket", "noise", "yamux", "macros"] }
futures = "0.3.28"
