[package]
name = "ping"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.51.0", features = ["ping", "tcp", "dns", "async-std", "websocket", "noise", "mplex", "yamux", "macros"] }
futures = "0.3.26"
