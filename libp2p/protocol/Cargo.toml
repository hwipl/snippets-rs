[package]
name = "protocol"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.54.1", features = ["async-std", "tcp", "tls", "noise", "yamux", "dns"] }
futures = "0.3.30"
void = "1.0.2"
futures-timer = "3.0.3"
log = "0.4.22"
