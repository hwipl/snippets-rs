[package]
name = "protocol"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.52.1", features = ["tcp", "dns", "async-std", "websocket", "noise", "yamux"] }
futures = "0.3.28"
void = "1.0.2"
futures-timer = "3.0.2"
log = "0.4.19"
