[package]
name = "gossip"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.53.1", features = ["async-std", "tcp", "tls", "noise", "yamux", "dns", "gossipsub"] }
futures = "0.3.29"
