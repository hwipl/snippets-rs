[package]
name = "gossip"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.51.0", features = ["gossipsub", "tcp", "dns", "async-std", "websocket", "noise", "mplex", "yamux"] }
futures = "0.3.26"
