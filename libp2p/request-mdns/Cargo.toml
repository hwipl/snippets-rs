[package]
name = "request-mdns"
version = "0.1.0"
authors = ["hwipl"]
edition = "2021"

[dependencies]
libp2p = { version = "0.51.1", features = ["request-response", "mdns", "tcp", "dns", "async-std", "websocket", "noise", "mplex", "yamux", "macros"] }
futures = "0.3.27"
async-trait = "0.1.66"
